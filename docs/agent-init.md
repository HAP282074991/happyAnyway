# 长期岗位 Agent：创建与接手

本文件是岗位创建、替换与接手的统一登记入口，和其他项目文档一起保存在根 docs/，不能只依赖聊天记忆。

## 创建入口

创建消息只要求读取对应岗位配置的实际绝对路径，不复制职责、权限、工作要求、阅读清单或进度摘要。岗位自身的接手要求及资料入口维护在 docs/agents/ 对应文件，动态进度仍以文件链接读取，避免维护两套规则。

根 Agent 已是项目经理，不在本招聘流程另行创建；docs/agents/ 只配置待招聘的五个岗位。目录存代码和文档，招聘岗位用长期独立任务承载。项目经理统一调配。

## 创建方式：逐个确认，逐个创建

- 用户已决定不批量创建。建议顺序为产品设计、后端开发、微信开发、审核、测试；用户可调整顺序。
- 用户说“初始化 agent”或提出创建某岗位时，先查看下方登记及现有任务，只处理当前岗位。仅讨论或修改说明不触发创建。
- 与用户明确五项：职责、决策与读写边界、接单输入、交付标准、协作及反馈关系。通用约定引用部门流程，个别岗位的补充经确认后写回 docs/agents/ 对应配置，避免两份规则。
- 记录本岗位职责与边界的确认依据；用户确认并授权创建后再执行。已有明确授权不重复询问；总体部门模式已通过不等于每个岗位的具体边界已确认。
- 查询项目和现有任务，匹配角色、项目与范围后复用，否则创建一个长期独立任务；遵循工具的环境要求，不以临时子 Agent 替代。
- 创建前确认岗位配置及其资料链接可被新任务读取，配置中的初始化要求齐全。创建消息只发送“请读取并遵循 <岗位配置绝对路径>。”；版本与源目录信息记在创建记录中。跨 worktree 未同步时使用实际可访问的配置来源路径，不在消息中重复规则。
- 收到创建结果和接手确认后更新本文件，再向用户报告。排队记为待就绪，失败如实记录，重试前检查以免重复；不自动创建下一个岗位。

## 岗位登记

2026-09-11：部门角色已确定，逐岗职责与边界待讨论；以下状态来自本任务已执行记录，不声称已全面核实其他历史任务。

| 岗位 | 职责与边界确认 | 当前正式任务 | 创建／接手状态 |
|---|---|---|---|
| 项目经理（非招聘岗位） | 已确认，见根 AGENTS.md | 当前根协调任务 | 本次会话已承担职责 |
| 产品设计 | 2026-09-13 已确认 V1 | 01a09a7b-8aba-7c21-a1ff-2e6cb093d7d7 | INIT-PD-001 已接手、已就绪，等待派工 |
| 后端开发 | 待逐项确认 | 待核实历史任务 | 尚未按当前岗位边界完成创建或复用 |
| 微信开发 | 待逐项确认 | 未登记 | 尚未由本次流程创建 |
| 审核 | 待逐项确认 | 未登记 | 尚未由本次流程创建 |
| 测试 | 待逐项确认 | 未登记 | 尚未由本次流程创建 |

历史后端任务 ID 01a089f9-d28f-7aa2-84ba-2d0f00b4c6c2、旧协调任务 ID 01a083ef-5932-7a01-a855-12a23e6db471 仅作查找线索，不直接作为当前岗位分配。

## 每次实际创建或复用后记录

在本文件末尾按日期追加一次记录，并更新上表当前任务。只为实际事件记录，不预建空档案。

- 岗位、日期、创建／复用／替换原因，以及用户确认该岗位职责和创建授权的依据。
- 实际任务标题、正式任务 ID、项目标识、运行位置与负责目录；排队阶段的临时标识单列，不冒充正式 ID。
- 当次采用的岗位配置路径及版本（未提交须说明），已确认的特殊边界与当前任务范围。
- 完整保留实际发送的简短创建／接手消息，登记所引用配置的版本及资料来源；不在消息中重复岗位规则，不记录敏感信息。
- 接手确认结果、缺失材料和待处理事项；尚未确认不能标记已就绪。

## 更换或恢复岗位

先读取原岗位记录、最新部门流程、项目进度索引及待接替任务文件，再核实旧任务实际状态。保留旧任务 ID、历次消息和结论，登记新任务接替关系；不能覆盖历史冒充原任务始终负责。

中断、停止核实、接单和恢复执行按[部门流程的长期岗位执行约定](workflow.md#长期岗位执行约定)处理。将未完成工作、成果版本、阻塞及下一步交给接手者，由其确认后更新当前映射；旧任务是否终止或归档按实际授权处理。长期保留岗位不代表一直运行。

## 当前创建消息示例

```text
请读取并遵循 D:\AIProject\happyAnyway\docs\agents\product-designer.md。
```

换机器或目录时替换为真实可访问路径。初始化行为由配置文件定义，不在创建消息中再次展开。

## 历史消息说明

以下 INIT-PD-001 长消息为历史创建记录，已按用户要求删去阶段限制措辞，不是今后的创建模板。2026-09-14 起采用上述配置入口方式；规则文件更新不代表已有 Agent 已重新读取或确认。

## INIT-PD-001 产品设计初始化（2026-09-13）

- 时间：2026-09-13 19:15 +08:00；下发者：当前根项目经理。
- 用户确认依据：项目经理列出五项职责边界并询问是否确认及授权创建，用户回复“对”。
- 查询现有任务后，未发现已登记的产品设计岗位；本次新建一个长期独立任务，不创建其他岗位。
- 项目：0b8fce99-29bf-43f6-b5b8-8515c01311c7；源目录：D:\AIProject\happyAnyway；采用默认独立 worktree，实际目录待结果。
- 配置：docs/agents/product-designer.md，2026-09-13 已确认 V1，未提交；源目录 HEAD：1568ef950f773f99ebef10331d99c38142c6d7fa。
- 状态：待发送；仅初始化阅读，无业务派工；正式任务 ID 与接手结果待追加。

创建消息（已删去阶段限制措辞）：

```text
初始化接手 INIT-PD-001：你是 happyAnyway 项目的长期产品设计 Agent，由项目经理统一调配。本次只阅读资料并简短确认接手，然后等待后续派工；不要自动盘点、编写需求、开发、审核、测试、提交、推送或发布。
项目源目录为 D:\AIProject\happyAnyway；工具将为你创建独立 Git worktree，请在回复中报告实际工作目录。源目录存在未提交规则，不能假定 worktree 中的规则已同步。此次岗位依据以源目录 D:\AIProject\happyAnyway\docs\agents\product-designer.md 的 2026-09-13 已确认 V1（未提交）为准，直接读取该绝对路径；同时读取源目录 AGENTS.md、docs/workflow.md、docs/progress.md、docs/agent-init.md、docs/requirements/README.md。初始化登记位于源目录 docs/agent-init.md 的 INIT-PD-001。
根 Agent 才是项目经理，你不继承项目经理写入权限。职责：将目标细化为需求、用户流程、交互、异常与验收条件；每次需求至少提出三个方案，说明做法、优缺点、成本、风险与推荐理由，由用户明确选择，不能以沉默代替确认。业务方向、范围和优先级由用户决定。可读取相关资料，后续仅在已登记派工范围内维护工作副本 docs/requirements/；不得修改业务代码、岗位配置或总进度。本次无文件修改授权。向项目经理反馈，与开发核对可行性、与测试核对可验证性。
当前 HARNESS-001 的规则与工程检查有历史记录，但独立审核、测试及用户验收尚未完成。HARNESS-002 待安排，尚未派工，规格引用 bootstrap-unpinned 不代表业务批准。源目录当前 HEAD 为 1568ef950f773f99ebef10331d99c38142c6d7fa，规则以本次未提交版本为准，历史检查不作为现状验证。
请确认读到的岗位版本、职责与读写边界、当前阶段、实际工作目录、是否存在资料缺失，并明确“等待派工”。如果材料不可访问请如实反馈，不自行补造或开展工作。
```
发送结果：创建工具已受理，等待 worktree 就绪。临时标识 client-new-thread:fef06734-762c-4cc1-aea4-2fd65ccef23d，hostId=local；尚未取得正式任务 ID，不标记已接手。

### INIT-PD-001 接手核实（2026-09-13）

- 用户要求“继续”后核对 Git、登记与最新进度，保留既有修改。任务列表未展示新任务；本地创建日志将上述临时标识映射到正式任务 ID，随后通过 wait_threads 核实完成结果，未重复创建。
- 实际任务标题：产品设计 Agent；正式任务 ID：01a09a7b-8aba-7c21-a1ff-2e6cb093d7d7；hostId：local；项目标识沿用本条创建记录。2026-09-13 19:20 +08:00 经 read_thread 再次核实标题、目录及接手回复。
- 实际工作目录：C:\Users\Administrator\.codex\worktrees\4f22\happyAnyway；HEAD：1568ef950f773f99ebef10331d99c38142c6d7fa。
- 接手回合：01a09a7b-8be9-71a0-a504-5e0e0913a77e，状态 completed，无错误；完成时间 2026-09-13 19:16:46 +08:00。接手消息 ID：msg_0ac5988ef9b2613c016aa68616ac8c87d095468987cf4060ce。
- Agent 确认已读取源目录已确认 V1（未提交）及全部六份指定资料，无缺失；确认需求设计、至少三个方案供用户选择、派工与读写边界。
- Agent 报告本次未修改文件，工作区无改动，但存在全局 Git ignore 读取权限提示；已明确“等待派工”。此项为岗位接手确认，不作为独立审核、测试或业务验收证据。
- 当前结果：产品设计岗位已就绪；未派发工作，未创建下一个岗位。后续跨 worktree 交付须明确版本与差异来源。
