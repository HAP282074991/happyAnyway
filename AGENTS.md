# happyAnyway 工作入口

## 组织与权限

- 单一 Git 仓库，代码分后端 happyAnyway-api、微信 happyAnyway-wechat；公共文档放 docs/，不按岗位新增子项目。
- 项目负责人（用户）决定目标、优先级、重大变更、预算和最终验收。
- 根任务是项目经理；产品设计、后端开发、微信开发、审核、测试为五个独立岗位任务，由项目经理调配。
- 职责、输入输出、放行条件和返工规则以[部门协作流程](docs/workflow.md)为准，已获用户确认。

## 接手与导航

- 先检查根 Git 状态、相关交接，保留已有修改；实现相关任务读取对应 specs-reference.json。
- 所有岗位读取部门流程中自己的职责和当前阶段；项目经理另读[管理职责](docs/project-management.md)。
- 产品设计读取[需求入口](docs/requirements/README.md)；后端读取[后端规则](happyAnyway-api/AGENTS.md)，微信读取[微信规则](happyAnyway-wechat/AGENTS.md)。
- 审核、测试读取待审／待测工程入口及其相关架构、规则和运行说明，不默认需要两端全部文档。
- 进度与验证证据查[项目进度](docs/progress.md)；岗位映射、创建消息和接替历史统一记在[创建与接手登记](docs/agent-init.md)，不只留在聊天。
- 用户说“初始化 agent”或明确要求建立团队时，读取[初始化说明](docs/agent-init.md)，先确认当前岗位职责与边界，只创建或复用该岗位并登记交接；不批量创建，不自动派发业务工作。

## 公共约束

- 只在任务授权范围实施，不覆盖已有修改，不根据猜测、占位目录或示例增加业务。
- 共同需求与验收由产品设计维护在 docs/requirements；业务决定由项目经理协调确认，各端不得私改。
- 变更先评估范围、时间与质量影响，再按权限决定；已授权的常规操作不重复确认。
- 不提交密钥、真实令牌、生产数据或本机私有配置；提交、推送、发布分别按授权执行。
- 不删除有效测试、削弱断言或伪造检查结果来过关。实现、自检、审核、测试、联调和用户验收分别记录。
- 关键阻塞、预计延期及时反馈；不受影响的工作继续。交付须有依据版本、成果位置、验证与未验证项。
- 修改行为、接口、命令或规则时同步权威文档和相关检查；复杂任务记录计划，收尾更新交接。共享文件由项目经理协调单一写入人。

## 当前阶段与检查

- **先完成 Harness 并验收，再开发业务。** 正式业务开发前确认需求和验收，锁定根需求提交及各端引用。
- 根目录执行 `./scripts/verify.ps1 -Scope docs` 检查文档；`./scripts/verify.ps1` 执行现有全部工程检查。
- 自动检查只验证已覆盖内容，不代替岗位独立审核、真实联调、真机验证或用户验收。
