# 0003：Harness 按职责拆分
日期：2026-09-10。
状态：按用户要求建立的讨论初版；用户已确认拆分方向，详细条款待后续讨论。

## 背景与决定
AGENTS 只保留统一规则与阅读路径。架构定义依赖；development/testing/review/api/database 分别定义专项规范。
同一要求只有一个权威位置，其他文档通过链接引用。HANDOFF 保存当前摘要，quality 保存验证覆盖和证据。

## 取舍
按任务渐进阅读，减少入口体积；需要维护索引和相对链接，避免文档之间产生不一致。
本轮不改变业务代码、依赖、安全配置或实际接口行为。

## 新的默认约定
规则文件提出未来业务接口默认分页、错误结构、类型表达与数据验证要求；这些不是已有实现。
新规则先作为开发/审查依据，自动化空缺明确记录，不为写规则就提前实现业务。

## 验证
扩展现有文档检查覆盖专项规则和交接文件；本轮执行文档检查，不重复 Java 构建。
