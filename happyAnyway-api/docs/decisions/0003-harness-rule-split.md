# Harness 按职责拆分

AGENTS 保存岗位入口与阅读路径；架构定义依赖；development/testing/review/api/database 分别定义专项规范。同一要求只有一个权威位置，其余通过链接引用。

HANDOFF 是接手入口，quality 说明验证覆盖与边界，执行计划及测试结果放入阶段任务记录。架构验收后保留本规则并清理过程记录。

分页、错误结构、类型表达等 API 约定是后续实现基线，不表示已有业务接口；检查空缺须明确列出，不为填文档提前开发业务。
