# 后端质量与执行状态
更新：2026-09-10。长期要求见 [规则索引](index.md)，当前任务见 [交接](HANDOFF.md)。

## 自动执行与限制
| 能力 | 状态 | 限制 |
|---|---|---|
| Java 编译与打包 | mvn verify 已接入 | 不证明业务正确 |
| 基础行为测试 | 3 项 | local、文档、默认拒绝；不含真实登录 |
| 架构检查 | 10 条 ArchUnit | 空业务层允许空匹配；不覆盖完整模块边界 |
| 文档结构检查 | scripts/check-structure.ps1 | 必需文件、相对路径链接；不检查语义、锚点、外链 |
| 规则反例验证 | 未完成 | 不能仅凭空层通过确认全部拦截能力 |
| MySQL / Flyway | 依赖和目录已准备 | 无业务 SQL、无真实数据库验证 |
| OpenAPI | local 可生成服务描述 | 无发布契约文件、无兼容检查 |
| CI 与合并门槛 | 未配置 | 本地检查尚不能强制阻止合并 |

## 人工执行的当前规则
- 开发日志与注释规范：已明确记录场景、等级、脱敏、Javadoc 与同步维护要求；暂无专门自动检查，现有代码未全量追补。
- 跨模块 Mapper、同层模块循环、模型边界及架构动态绕过。
- 事务语义、资源权限、日志脱敏、依赖引入理由。
- 契约兼容性、迁移方案、完成标准与交接更新。
- docs/rules 下的新要求是实现与审查规范，不能将文件存在等同于已自动强制。
- Service 接口/Impl 配对与调用方不得依赖 impl：用户已确认，当前人工审查，尚未增加专门 ArchUnit 检查。

## 待实现能力
真实 MySQL 账号/连接、业务接口与会话、统一错误/requestId、契约版本化、CI、部署与恢复验证。
后续接口的分页和错误约定尚未实现；根据具体业务落地并测试。

## 验证记录
- 2026-09-09：历史 mvn verify 构建成功，13 项测试通过（3 行为 + 10 架构），无提交号，未提交工作区。
- 更早基础环境验证：JAR 和 mvn local 启动、健康检查成功；Swagger 与默认拒绝路径完成过 HTTP 验证，临时进程已结束。
- 2026-09-10 本轮：powershell -NoProfile -File scripts/check-structure.ps1 通过，覆盖新规则、交接与全部相对文档链接；Java、pom 和运行配置未改动，未重跑 Maven。
- 本轮脚本隔离验证：完整目录样例通过；缺少必需文件、失效链接均被拒绝。临时样例位于被忽略的 target 内，不进入应用源码。

## 维护方式
每次验证记录日期、命令、代码状态、结果及未验证项。新证据补充或取代旧结论，不把旧记录重写成新验证。

## 2026-09-10 独立工作区迁移验证
仓库位于 D:\AIProject\happyAnyway。Git 元数据与原始文件迁移校验通过。新路径下前端 npm run verify 及后端 mvn verify（13 项测试）通过；共享规格和文档检查通过。真实 MySQL 与真机未验证。
