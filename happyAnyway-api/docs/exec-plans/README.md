# 执行计划

小改动不强制写计划。跨层、跨模块、多阶段或涉及兼容迁移的工作，在 active/ 记录可接续计划。

## 必要内容
- 目标、用户授权范围及不在范围内的事项。
- 关联需求、适用专项规则与验收条件。
- 步骤、当前进度、决策、失败和未验证项。
- 验证命令及结果；任务结束后记录剩余事项。

完成后移入 completed/；阻塞时保留 active/ 并明确缺少的条件，不能因结束对话就标记完成。
交接摘要写入 [HANDOFF](../HANDOFF.md)，详细记录留在计划中。
