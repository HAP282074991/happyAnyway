# Harness 规则分拆
日期：2026-09-10。
目标：AGENTS 只保留统一规则与索引，独立维护开发、测试、审查、API、数据库规则，补充交接。
范围：后端文档和文档结构检查；不修改业务、依赖、运行安全策略。
步骤：核对现状 → 编写分域规则 → 重写导航与交接 → 检查链接和必要文件 → 归档。
验收：权威来源清晰，无错误链接，建议/已实现/历史验证区分，前端不改动。
结果：五份专项规则、精简入口、架构职责、交接、质量状态及索引已完成；更新产品名称和契约/迁移入口，避免多份规则重复。
验证：文档检查通过；隔离样例确认正常结构通过、缺少必需文件与失效链接失败。
未验证：本轮无 Java/配置变更，不重跑 Maven；真实数据库和接口验证仍未完成。
后续：与用户讨论条款取舍；详细规则作为本轮提案基线，不声称对应能力均已实现。
